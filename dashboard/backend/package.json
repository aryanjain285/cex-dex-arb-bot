{
  "name": "dashboard-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Relays the bot's Redis pub/sub messages to the dashboard frontend over WebSocket",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "redis": "^4.6.14",
    "socket.io": "^4.7.5"
  },
  "scripts": {
    "start": "node server.js"
  }
}
